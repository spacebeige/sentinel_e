# ── Core API Framework ──
fastapi
uvicorn[standard]
python-multipart
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ── Cloud LLM Clients ──
aiohttp
requests
python-dotenv
tavily-python

# ── Database ──
sqlalchemy>=2.0.0
asyncpg>=0.29.0
redis>=5.0.0

# ── Lightweight Analysis ──
numpy
scikit-learn
networkx
textblob
tenacity

# ── Security & Auth ──
PyJWT>=2.8.0
bcrypt>=4.0.0

# ── v2 Architecture ──
aiosqlite>=0.20.0

# ── Compressed Pipeline (Sentinel-E Sigma) ──
langgraph>=0.4.0
langchain-core>=0.3.0
google-genai>=1.0.0
beautifulsoup4>=4.12.0
tiktoken>=0.7.0
httpx>=0.27.0

# ── Optional: Heavy ML (only install when USE_LOCAL_INGESTION=true) ──
# Uncomment these for local development with embeddings/RAG:
# torch
# sentence-transformers
# faiss-cpu
# langchain
# langchain-community
# huggingface_hub
# pypdf
# PyMuPDF
# streamlit
# faster-whisper
# edge-tts
# matplotlib
# seaborn
