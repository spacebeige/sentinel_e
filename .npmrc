# npm configuration for Sentinel-E

# Use Node.js LTS versions minimum
legacy-peer-deps=false

# Prefer offline for consistent builds
prefer-offline=true

# Ensure audit checks work properly
audit=true

# Use latest npm
update-notifier=true
